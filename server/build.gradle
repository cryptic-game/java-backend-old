plugins {
    id 'java'
}

dependencies {
    implementation project(":java-base")
}

def mainClassName = "net.cryptic_game.backend.server.App"

jar {
    manifest {
        attributes "Main-Class": "$mainClassName"
    }
}
